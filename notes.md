## Notes:
- When searching until the target amount of rollouts have been made is enabled for AlphaProof inference, we want to encourage the search to search in different areas of the tree instead of always expanding nodes on the same branch that have already been solved. To achieve this we can give solved nodes a value penalty score like -40, to discourage the search from using the same branch. This is perfect since it mostly affects the average value of the higher up nodes close to this node. For the root node it would only have a small impact.
- I need to change the plan to reflect the above and change which search metrics are calculated from the tree. The AND nodes of the tree should be removed by setting them equal to the child node they have with the lowest value. This creates an OR only tree with the most difficult subproblems. Then from this tree each leaf node corresponds to a solution path. This can either be soved or not solved. That means we can calculate the solve fraction as the fraction of leaf nodes that are solved.